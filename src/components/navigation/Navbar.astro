<nav class="fixed md:h-[calc(100vh-4rem)] flex flex-col justify-start md:py-8 z-50 group/nav">
    <div class="w-6 md:w-20 h-20 md:h-20 grid place-items-center relative cursor-pointer z-20 -ml-6 md:ml-0 hover:ml-0 transition-all duration-300 group-hover/nav:ml-0" role="button" tabindex="0" aria-label="Toggle menu" id="nav-toggle">
        <div class="w-6 h-14 md:w-7 md:h-7 md:bg-[#ff3d00] relative transition-all duration-300 ease-in-out md:rounded-full flex items-center justify-end md:justify-center group-hover/nav:scale-110 group-hover/nav:shadow-[0_0_30px_rgba(255,61,0,0.3)]" aria-hidden="true">
            <span class="text-[#ff3d00] text-lg md:text-2xl font-light opacity-40 transition-all duration-300 rotate-180 pr-1 md:p-0 group-hover/nav:opacity-100 group-hover/nav:translate-x-0.5 group-hover/nav:rotate-90 md:absolute md:-right-7 md:top-1/2 md:-translate-y-1/2">›</span>
        </div>
    </div>
    <div id="nav-overlay" class="fixed inset-0 bg-black/50 backdrop-blur-sm opacity-0 pointer-events-none transition-opacity duration-300 md:hidden"></div>
    <div class="fixed left-0 top-0 h-screen flex flex-col justify-center gap-6 pl-8 w-64 bg-gradient-to-r from-black/97 to-transparent -translate-x-full transition-all duration-500 ease-in-out group-hover/nav:translate-x-0 group-hover/nav:opacity-100 z-10 backdrop-blur-md opacity-0">
        <a href="#services" class="text-white/70 no-underline text-sm tracking-widest font-medium relative pl-0 hover:text-white hover:pl-4 transition-all duration-300 before:content-[''] before:absolute before:left-0 before:top-1/2 before:-translate-x-5 before:w-0 before:h-px before:bg-[#ff3d00] hover:before:w-6 before:transition-all before:duration-300">SERVICES</a>
        <a href="#contact" class="text-white/70 no-underline text-sm tracking-widest font-medium relative pl-0 hover:text-white hover:pl-4 transition-all duration-300 before:content-[''] before:absolute before:left-0 before:top-1/2 before:-translate-x-5 before:w-0 before:h-px before:bg-[#ff3d00] hover:before:w-6 before:transition-all before:duration-300">CONTACT</a>
    </div>
</nav>

<script>
    const nav = document.querySelector('.group\\/nav') as HTMLElement;
    const overlay = document.getElementById('nav-overlay');
    const toggle = document.getElementById('nav-toggle');
    let isOpen = false;

    function toggleMenu(open: boolean) {
        isOpen = open;
        if (open) {
            nav.classList.add('menu-open');
            overlay?.classList.add('opacity-100', 'pointer-events-auto');
        } else {
            nav.classList.remove('menu-open');
            overlay?.classList.remove('opacity-100', 'pointer-events-auto');
        }
    }

    toggle?.addEventListener('click', (e) => {
        e.stopPropagation();
        toggleMenu(!isOpen);
    });

    overlay?.addEventListener('click', () => {
        toggleMenu(false);
    });

    // Handle hover for desktop
    if (window.matchMedia('(min-width: 768px)').matches) {
        nav?.addEventListener('mouseenter', () => toggleMenu(true));
        nav?.addEventListener('mouseleave', () => toggleMenu(false));
    }
</script>

<style>
    .menu-open {
        pointer-events: auto;
    }
    
    @media (max-width: 767px) {
        .group\/nav:not(.menu-open) {
            pointer-events: none;
        }
        .group\/nav:not(.menu-open) #nav-toggle {
            pointer-events: auto;
        }
    }
</style> 