---
---
<section class="hero">
    <div class="hero-content">
        <h1>BREAKING<br/>DIGITAL<br/>BOUNDARIES</h1>
        <div class="hero-line"></div>
    </div>
    <div class="scroll-indicator">SCROLL ↓</div>
</section>

<style>
    .hero {
        min-height: 90vh;
        display: flex;
        align-items: center;
        position: relative;
    }

    .hero-content {
        display: flex;
        align-items: center;
        gap: 2rem;
    }

    h1 {
        font-size: clamp(3rem, 8vw, 6rem);
        font-weight: 900;
        line-height: 0.9;
        margin: 0;
    }

    .hero-line {
        width: 30vw;
        height: 2px;
        background: #FF5F1F;
        transform: translateY(-50%);
    }

    .scroll-indicator {
        position: absolute;
        bottom: 2rem;
        left: 0;
        writing-mode: vertical-rl;
        transform: rotate(180deg);
        font-size: 0.875rem;
        letter-spacing: 0.2em;
    }
</style> 