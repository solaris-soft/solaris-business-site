---
---
<div class="container">
    <nav class="nav">
        <div class="sun-logo">
            <div class="sun"></div>
        </div>
        <div class="menu">
            <a href="#philosophy" class="menu-item">PHILOSOPHY</a>
            <a href="#work" class="menu-item">WORK</a>
            <a href="#contact" class="menu-item">CONTACT</a>
        </div>
    </nav>

    <header class="hero">
        <div class="hero-content">
            <h1>SOL<span class="gradient-text">ARIS</span></h1>
            <div class="hero-line"></div>
            <p class="manifesto">We craft digital experiences that embrace human connection in the technological age.</p>
        </div>
        <div class="coordinates">34°02'N 118°12'W</div>
    </header>

    <section class="services">
        <div class="service-grid">
            <div class="service-item">
                <span class="service-number">001</span>
                <div class="service-content">
                    <h2>HUMAN INTERFACE</h2>
                    <p>Digital products that feel natural</p>
                </div>
            </div>
            <div class="service-item">
                <span class="service-number">002</span>
                <div class="service-content">
                    <h2>DIGITAL CRAFT</h2>
                    <p>Precision in every pixel</p>
                </div>
            </div>
            <div class="service-item">
                <span class="service-number">003</span>
                <div class="service-content">
                    <h2>BRAND ESSENCE</h2>
                    <p>Identity through simplicity</p>
                </div>
            </div>
        </div>
    </section>
</div>

<style>
    .container {
        display: grid;
        grid-template-columns: 120px 1fr;
        min-height: 100vh;
        color: white;
        padding: 2rem;
        gap: 4rem;
    }

    .nav {
        position: fixed;
        height: calc(100vh - 4rem);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        padding: 2rem 0;
    }

    .sun-logo {
        width: 80px;
        height: 80px;
        display: grid;
        place-items: center;
        position: relative;
    }

    .sun {
        width: 40px;
        height: 40px;
        background: #FF3D00;
        border-radius: 50%;
        position: relative;
        box-shadow: 0 0 50px rgba(255, 61, 0, 0.3);
        animation: pulse 4s ease-in-out infinite;
    }

    @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }

    .menu {
        display: flex;
        flex-direction: column;
        gap: 3rem;
        writing-mode: vertical-rl;
        transform: rotate(180deg);
    }

    .menu-item {
        color: white;
        text-decoration: none;
        font-size: 0.75rem;
        letter-spacing: 0.2em;
        transition: color 0.3s;
        font-weight: 500;
    }

    .menu-item:hover {
        color: #FF3D00;
    }

    .hero {
        grid-column: 2;
        height: 90vh;
        display: flex;
        align-items: center;
        justify-content: space-between;
        position: relative;
    }

    .hero-content {
        max-width: 90%;
    }

    h1 {
        font-size: clamp(5rem, 20vw, 14rem);
        font-weight: 900;
        line-height: 0.8;
        margin: 0;
        letter-spacing: -0.02em;
    }

    .gradient-text {
        background: linear-gradient(90deg, #FF3D00, #FF8A00);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero-line {
        width: 150px;
        height: 4px;
        background: #FF3D00;
        margin: 2rem 0;
    }

    .manifesto {
        font-size: clamp(1rem, 2vw, 1.5rem);
        max-width: 600px;
        line-height: 1.4;
        color: #888;
    }

    .coordinates {
        writing-mode: vertical-rl;
        font-size: 0.75rem;
        letter-spacing: 0.2em;
        color: #444;
        font-family: monospace;
    }

    .services {
        grid-column: 2;
        padding: 4rem 0;
    }

    .service-grid {
        display: grid;
        gap: 4rem;
    }

    .service-item {
        display: grid;
        grid-template-columns: 80px 1fr;
        gap: 2rem;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.02);
        transition: all 0.3s ease;
    }

    .service-item:hover {
        background: rgba(255, 61, 0, 0.05);
        transform: translateX(20px);
    }

    .service-number {
        font-family: monospace;
        color: #FF3D00;
        font-size: 0.875rem;
    }

    .service-content h2 {
        font-size: 1.5rem;
        margin: 0 0 0.5rem 0;
        font-weight: 700;
    }

    .service-content p {
        color: #666;
        font-size: 0.875rem;
        margin: 0;
    }

    @media (max-width: 768px) {
        .container {
            grid-template-columns: 1fr;
            gap: 2rem;
        }

        .nav {
            position: relative;
            height: auto;
            flex-direction: row;
            align-items: center;
            padding: 0;
        }

        .menu {
            writing-mode: horizontal-tb;
            transform: none;
            flex-direction: row;
        }

        .hero {
            grid-column: 1;
            height: auto;
            flex-direction: column;
            gap: 2rem;
        }

        .services {
            grid-column: 1;
        }
    }
</style> 